# Smaller/faster models (gpt-4.1-mini, gpt-5-mini) recommended for browser-agent tasks.
# Use larger models (gpt-4.1, gpt-5) only when complex reasoning is required.
OPENAI_API_KEY="sk-proj-openaikey"
OPENAI_MODEL="gpt-4o"
NORMALIZER_MODEL="gpt-4o-mini"
